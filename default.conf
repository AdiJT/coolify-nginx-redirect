events {}
error_log /dev/stdout info;

http {
    access_log /dev/stdout;
    map $http_upgrade $connection_upgrade_keepalive {
        default upgrade;
    }
    
    server {
        listen 80;
        server_name _;
        rewrite rewrite ^/(.*)$ https://ilkom.undana.ac.id/$1;
    }
}